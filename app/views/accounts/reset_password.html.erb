<% @title_tag = "Reset password" %>

<div class="box box--sm box--miniform" data-controller="reset_password">
	<p>
		To begin the password reset process, please enter your email below. Here by mistake?
		<%= link_to "Log in", "/users/sign_in" %>.
	</p>
	<%= form_with(
				url: send_password_reset_email_path,
				html: {
					class: "form form--vertical",
					data: { turbo: false }
				}) do |f| %>
		<%= f.email_field(
					:email,
					autofocus: true,
					autocomplete: "email",
					placeholder: "Email address",
					class: "input input--email",
					autocapitalize: "off") %>
		<%= f.submit(
					"Reset",
					class: "btn btn--submit") %>
	<% end %>
</div>

